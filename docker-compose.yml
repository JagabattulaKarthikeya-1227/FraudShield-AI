version: '3.8'

services:
  # -----------------------------------------
  # Frontend React App (Production Build)
  # -----------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - fraudshield_net

  # -----------------------------------------
  # Backend Flask API (Gunicorn)
  # -----------------------------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=mysql+pymysql://fraud_user:fraud_pass@mysql_db:3306/fraudshield
      - REDIS_URL=redis://redis_cache:6379/0
      - JWT_SECRET_KEY=production_secret_do_not_use_in_real_life
    depends_on:
      mysql_db:
        condition: service_healthy
      redis_cache:
        condition: service_started
    networks:
      - fraudshield_net

  # -----------------------------------------
  # Celery Background Worker
  # -----------------------------------------
  celery_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A app.core.celery_app.celery worker --loglevel=info
    environment:
      - DATABASE_URL=mysql+pymysql://fraud_user:fraud_pass@mysql_db:3306/fraudshield
      - REDIS_URL=redis://redis_cache:6379/0
    depends_on:
      - redis_cache
      - mysql_db
    networks:
      - fraudshield_net

  # -----------------------------------------
  # MySQL Database
  # -----------------------------------------
  mysql_db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=root_pass
      - MYSQL_DATABASE=fraudshield
      - MYSQL_USER=fraud_user
      - MYSQL_PASSWORD=fraud_pass
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fraudshield_net

  # -----------------------------------------
  # Redis Broker & Cache
  # -----------------------------------------
  redis_cache:
    image: redis:7.2-alpine
    volumes:
      - redis_data:/data
    networks:
      - fraudshield_net

volumes:
  mysql_data:
  redis_data:

networks:
  fraudshield_net:
    driver: bridge
